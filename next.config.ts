import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 /*basePath: "/nuevosub", // Ajusta al nombre del subdirectorio en tu subdominio
  assetPrefix: "https://nuevosub.contigo-voy.com", // URL completa del subdominio
  trailingSlash: true, // Opcional: agrega barras al final de las rutas*/
};

export default nextConfig;

